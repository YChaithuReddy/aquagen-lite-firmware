/** AquaGen Lite — secrets template. Copy this to iot_secrets.h and fill in real values.
 *  iot_secrets.h is gitignored; this .template is committed so others know the format. */
#ifndef IOT_SECRETS_H
#define IOT_SECRETS_H

// WireGuard agent-registration auth key (POST /wg/api/agent/register on the FluxGen panel server).
#define WG_AUTH_KEY "REPLACE_WITH_WIREGUARD_REGISTER_AUTH_KEY"

#endif // IOT_SECRETS_H
